# Cloud Functions Environment Variables

# API Key do bridge server (deve ser a mesma configurada no bridge server)
# Alternativamente, pode ser configurado no Firestore (bridgeConfig)
BRIDGE_API_KEY=your_secret_api_key_here_change_this
